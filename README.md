# Rental Manager API

FastAPI backend for property rental management.

## Tech Stack
- FastAPI + Uvicorn
- PostgreSQL + SQLAlchemy
- JWT Authentication
- Booking system

## Run locally
```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
